module.exports = class RadioFormElement {
    constructor(title, component, prop) {
        this.title = title;
        // this.subtitle = subtitle;
        // this.info = info;
        this.component = component;
        this.prop = prop;
    }
}