/*
    Vuetify is a VueJS based component library. It provides a number of pre-styled
    standard components such as buttons, carousels, cards etc. The components
    are implemented in VueJS, so can be easily customized and their behaviour
    controlled.
*/


import Vue from 'vue';
import Vuetify from 'vuetify';
import 'vuetify/dist/vuetify.min.css'

Vue.use(Vuetify);

export default new Vuetify({});
